pub mod app;
pub mod storage;
pub mod http_client;
pub mod utils;
pub mod hcs_endpoints;
pub mod hcs_api;

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
